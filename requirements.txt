# Vercel serverless dependencies (lightweight — API functions only)
# The prices API uses only urllib (built-in), so no extra deps needed.
# This file exists to satisfy Vercel's Python runtime requirement.
